---
import { YouTube } from 'astro-embed';
import { CONTACT } from '../lib/contact';
import { fetchLatestVideos } from '../lib/youtube';

// Fetch videos from YouTube RSS feed
const videos = await fetchLatestVideos();

// Cache for 24 hours on Vercel's edge network (shared cache)
// s-maxage: cache on edge for 24 hours, shared among all users
// stale-while-revalidate: serve stale content while revalidating
Astro.response.headers.set(
  'Cache-Control',
  'public, s-maxage=86400, stale-while-revalidate=86400'
);
---
{videos.length > 0 ? (
<section id="videos" class="section bg-linear-to-br from-primary to-primary-dark" aria-labelledby="videos-heading">
  <div class="container">
    <header class="text-center mb-16">
      <h2 id="videos-heading" class="text-4xl md:text-5xl font-heading font-bold text-black mb-6">
        Últimos Videos de Deportivo Norte
      </h2>
      <p class="text-lg text-black/80 max-w-2xl mx-auto">
        Seguí todas las novedades del Club Deportivo Norte de Mar del Plata. Partidos de fútbol, entrenamientos y momentos únicos de nuestra familia norteña.
      </p>
    </header>

    
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto">
        {videos.map((video) => (
          <article class="bg-black">
            <YouTube id={video.id} />
            <header class="p-6">
              <h3 class="text-lg font-heading font-bold text-white">{video.title}</h3>
            </header>
          </article>
        ))}
      </div>

    <div class="text-center mt-12">
      <a
        href={CONTACT.social.youtube}
        target="_blank"
        rel="noopener noreferrer"
        class="btn-primary-dark"
      >
        Ver Todos los Videos
      </a>
      <p class="text-black/70 mt-6">
        Conocé más sobre <a href="#historia" class="text-black underline hover:no-underline font-bold">nuestra historia</a> o
        <a href="#asociate" class="text-black underline hover:no-underline font-bold">asociate al club</a>.
      </p>
    </div>
  </div>
</section>
) : (
  <>
  </>
)}
