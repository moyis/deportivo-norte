---
import logo from '../assets/logo.svg';
import Instagram from '../assets/instagram.svg';
import Facebook from '../assets/facebook.svg';
import YouTube from '../assets/youtube.svg';
import Email from '../assets/email.svg';
import GPS from '../assets/gps.svg';
import { Image } from 'astro:assets';

const currentYear = new Date().getFullYear();

const sections = [
  { label: 'Asociate', href: '#asociate' },
  { label: 'Historia del Club', href: '#historia' },
  { label: 'Preguntas Frecuentes', href: '#faq' },
  // { label: 'Sponsors', href: '#sponsors' },
];

const socialLinks = [
  { label: 'Instagram', href: 'https://www.instagram.com/depnorte/', icon: 'instagram' },
  { label: 'Facebook', href: 'https://www.facebook.com/depnorteoficial', icon: 'facebook' },
  { label: 'YouTube', href: 'https://www.youtube.com/@DeportivoNorte', icon: 'youtube' },
  { label: 'depnorte@gmail.com', href: 'mailto:depnorte@gmail.com', icon: 'email' },
];
---

<footer id="contacto" class="bg-black border-t border-white/10 py-16" role="contentinfo">
  <div class="container py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
      <div class="lg:col-span-2">
        <a href="/" class="flex items-center gap-3 mb-6" aria-label="Club Deportivo Norte">
          <Image src={logo} alt="Logo Club Deportivo Norte" class="w-auto h-14" />
          <div>
            <span class="font-heading text-[#FFCC00] text-2xl font-bold uppercase block">Deportivo Norte</span>
            <span class="text-gray-400 text-sm">Desde 1937</span>
          </div>
        </a>
        <p class="text-gray-400 leading-relaxed mb-6 max-w-md">
          El Club Deportivo Norte es una institución social y deportiva de Mar del Plata, 
          Argentina.
        </p>
        <address class="not-italic text-gray-400 space-y-2">
          <p class="flex items-start gap-3">
            <GPS/>
            <span>Estadio Carlos H. Miori<br />Mar del Plata, Buenos Aires, Argentina</span>
          </p>
        </address>
      </div>

      <nav aria-label="Enlaces del pie de página">
        <h3 class="font-heading font-bold text-white uppercase text-lg mb-6">Enlaces</h3>
        <ul class="space-y-3" role="list">
          {sections.map((link) => (
            <li>
              <a href={link.href} class="text-gray-400 hover:text-[#FFCC00] transition-colors inline-block">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>

      <div>
        <h3 class="font-heading font-bold text-white uppercase text-lg mb-6">Seguinos</h3>
        <ul class="space-y-3" role="list">
          {socialLinks.map((social) => (
            <li>
              <a href={social.href} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-[#FFCC00] transition-colors inline-flex items-center gap-2">
                {social.icon === 'instagram' && (
                  <Instagram />
                )}
                {social.icon === 'facebook' && (
                  <Facebook />
                )}
                {social.icon === 'youtube' && (
                  <YouTube />
                )}
                {social.icon === 'email' && (
                  <Email />
                )}
                {social.label}
              </a>
            </li>
          ))}
        </ul>
        <a href="https://docs.google.com/forms/d/e/1FAIpQLSdjUjWomnygFy_uXQyg9LXq-WRbDDpTOw2sqENAi1cxbYOBsw/viewform" target="_blank" rel="noopener noreferrer" class="btn-primary mt-8 w-full text-center">¡Asociate Ahora!</a>
      </div>
    </div>
  </div>
</footer>
